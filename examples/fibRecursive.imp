int fib(int n) {
    if (n == 0 || n == 1) {
        return 1;
    };
    elif (n < 0) {
        skip; -- force error at runtime
    };
    else {
        return fib(n - 1) + fib(n - 2);
    };
};

var n := 0;
print fib(n);

while (_T) {
    print "\n";
    n := n + 1;
    print fib(n);

    if (n >= 20) {
        break;
    };
    continue;
    print "Hideous curse words\n"
};